@using BlazorCssTransitions.UnitTests.Tests.AnimatedContent

@inherits BCTTestContext

@code {
    private RenderFragment<States> SampleContent()
        => state => @<span>@(state.ToString())</span>;

    private string SampleContentText(States state)
        => $"<span>{state}</span>";


    private RenderFragment<States> SampleCountableContent()
        => state => @<SimpleElementForLifespanTesting State=@state />;
}
